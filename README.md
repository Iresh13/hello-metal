This project is built using Expo

To run it locally :

1. create a new .env file
2. Add this to the env file

EXPO_PUBLIC_API_URL=https://dog.ceo/api/

3. start expo server:
   npx expo start --dev-client

4. Test it on your simulator
